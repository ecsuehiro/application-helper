"use strict"

const React = require('react')
const ReactDOM = require('react-dom')
const Base = require('./application.base')

ReactDOM.render(
    <Base></Base>,
    document.getElementById('content')
)